const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let snapshot={jobs:[]}, current=null;
const fields=['title','author','narrator','year','series','series_number','genre','publisher','copyright','isbn','asin','language','description','cover_url'];
const label=s=>s.replaceAll('_',' ').replace(/^./,c=>c.toUpperCase());
function notice(text){$('#notice').textContent=text;$('#notice').hidden=false;setTimeout(()=>$('#notice').hidden=true,9000)}
async function api(path,method='GET',body){const r=await fetch('/api'+path,{method,headers:body===undefined?{}:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw Error(typeof data.detail==='string'?data.detail:JSON.stringify(data.detail));return data}
async function act(fn){try{await fn()}catch(e){notice(e.message)}}
function screen(name){for(const id of ['queue','detail','settings','grouping'])$('#'+id).hidden=id!==name;window.scrollTo(0,0)}
function duration(s){return `${Math.floor(s/3600)}h ${Math.round(s%3600/60)}m`}
function image(url,cls=''){return url&&/^https:\/\//.test(url)?`<img class="${cls}" src="${esc(url)}" alt="Book cover" referrerpolicy="no-referrer" loading="lazy">`:''}
async function queue(){snapshot=await api('/jobs');screen('queue');drawQueue();const h=await api('/health');const heartbeat=Number(h.runtime.worker_heartbeat||0);$('#worker-status').textContent=heartbeat?'Worker connected · '+new Date(heartbeat*1000).toLocaleTimeString():'Waiting for worker';const errors=Object.entries(h.runtime).filter(([k,v])=>k.endsWith('_error')&&v);if(errors.length)notice(errors.map(([k,v])=>label(k)+': '+v).join(' · '))}
function drawQueue(){const states=['REVIEW','READY','PROCESSING','COMPLETE','ERROR'];$('#counts').innerHTML=states.map(s=>`<div class="count"><span>${({REVIEW:'Needs review',READY:'Ready / scheduled',PROCESSING:'Processing',COMPLETE:'In your library',ERROR:'Needs attention'})[s]}</span><strong>${snapshot.jobs.filter(j=>j.status===s).length}</strong></div>`).join('');const filter=$('#filter').value;const jobs=snapshot.jobs.filter(j=>!filter||j.status===filter);$('#books').innerHTML=jobs.length?jobs.map(j=>`<article class="book"><div class="info"><h3>${esc(j.body.metadata.title||'Untitled book')}</h3><span>${esc(j.body.metadata.author||'Author needed')}</span><p class="muted">${j.body.files.length} file(s) · ${duration(j.body.embedded.duration)} · ${esc(j.body.metadata.narrator||'Narrator unknown')}</p></div><span class="badge ${j.status}">${j.status}</span><button data-open="${j.id}">${j.status==='COMPLETE'?'View book':'Open review'}</button></article>`).join(''):`<div class="empty"><h2>${filter?'No books in this state':'Your next listen starts here'}</h2><p>Set your folders in Settings, then check completed downloads. Each source package becomes one or more book jobs for inspection and review.</p><button id="empty-settings">Open Settings</button></div>`;$('#packages').innerHTML=snapshot.packages.filter(p=>p.status!=='INSPECTED').map(p=>`<div class="${p.error?'error':'panel'}"><strong>${esc(p.status)}</strong> · ${esc(p.source)}${p.error?`<p>${esc(p.error)}</p><button data-package-retry="${p.id}">Retry inspection</button>`:''}</div>`).join('');$('#scans').innerHTML=snapshot.scans.length?`<details><summary>Audiobookshelf scan requests</summary>${snapshot.scans.map(s=>`<p>${esc(s.status)} ${esc(s.error||'Scan request accepted')}</p>`).join('')}</details>`:'';document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>act(()=>detail(b.dataset.open)));document.querySelectorAll('[data-package-retry]').forEach(b=>b.onclick=()=>act(async()=>{await api(`/packages/${b.dataset.packageRetry}/retry`,'POST',{});await queue()}));if($('#empty-settings'))$('#empty-settings').onclick=()=>act(showSettings)}
async function detail(id){current=await api('/jobs/'+id);const j=current,b=j.body,m=b.metadata,editable=['REVIEW','ERROR'].includes(j.status);screen('detail');$('#detail').innerHTML=`<button id="back">← Book queue</button><div class="heading"><div><p class="eyebrow">BOOK REVIEW</p><h1>${esc(m.title)}</h1><span class="badge ${j.status}">${j.status}</span></div><button id="group" ${editable?'':'disabled'}>Review package grouping</button></div>${j.error?`<div class="error">${esc(j.error)}</div>`:''}${b.lookup_error?`<div class="warning">${esc(b.lookup_error)}</div>`:''}${j.output?`<p class="preview">Published to ${esc(j.output)}</p>`:''}<div class="split"><div><div class="panel"><h2>Source audio</h2><p>${duration(b.embedded.duration)} · ${mediaMode(b.files)}</p>${b.files.map((f,i)=>`<div class="file"><strong>${i+1}.</strong> ${esc(f.relative)}<br><span class="muted">${duration(f.duration)} · ${esc(f.codec)}</span>${f.warning?`<div class="warning">${esc(f.warning)}</div>`:''}</div>`).join('')}<p><label><input id="group-confirm" type="checkbox" ${b.grouping_confirmed?'checked':''} ${editable?'':'disabled'}>These files are one book, in the correct order</label></p></div><div class="panel"><h2>Find the right edition</h2><label>Metadata source<select id="provider"><option>Audible</option><option>Google Books</option><option>Open Library</option></select></label><label>Title<input id="query" value="${esc(m.title)}"></label><label>Author<input id="search-author" value="${esc(m.author)}"></label><label>ASIN or Audible URL (optional)<input id="asin" placeholder="B0… or https://www.audible.com/pd/…"></label><div class="actions"><button id="search" ${editable?'':'disabled'}>Search</button><button id="embedded" ${editable?'':'disabled'}>Use existing metadata</button></div><div id="candidates">${b.candidates.map((c,i)=>`<div class="candidate">${image(c.cover_url)}<h3>${esc(c.title)}</h3><p>${esc(c.author)}<br><small>${esc(c.narrator||'Narrator unavailable')} · ${c.duration?duration(c.duration):'Print-book metadata'}</small></p><strong>${c.confidence.total}% evidence score</strong><details><summary>Why this score?</summary><div class="signals">${c.confidence.signals.map(s=>`${esc(label(s.field))}: ${s.points}/${s.maximum} — ${esc(s.reason)}`).join('<br>')}</div></details><button data-candidate="${i}" ${editable?'':'disabled'}>Use this metadata</button></div>`).join('')||'<p class="muted">Search for candidates, paste an ASIN, or enter metadata yourself.</p>'}</div></div></div><div><div class="panel"><h2>Final metadata</h2><form id="metadata-form"><div class="field-grid">${fields.map(f=>`<label class="${['description','cover_url'].includes(f)?'wide':''}">${esc(label(f))}${f==='description'?`<textarea name="${f}" ${editable?'':'disabled'}>${esc(m[f])}</textarea>`:`<input name="${f}" value="${esc(m[f])}" ${editable?'':'disabled'}>`}<small>${esc(b.provenance[f]||'Not set')}</small></label>`).join('')}</div><h3>Cover selection</h3><div class="cover-choice"><label><input type="radio" name="cover" value="embedded" ${b.cover_choice==='embedded'?'checked':''}>Embedded artwork</label><label>${image(m.cover_url)}<input type="radio" name="cover" value="provider" ${b.cover_choice==='provider'?'checked':''}>Metadata cover</label><label><input type="radio" name="cover" value="none" ${b.cover_choice==='none'?'checked':''}>No cover</label>${b.covers.map((p,i)=>`<label><img src="/api/jobs/${j.id}/cover/${i}" alt="Local cover ${i+1}"><input type="radio" name="cover" value="local:${i}" ${b.cover_choice===`local:${i}`?'checked':''}>Local ${i+1}</label>`).join('')}</div><div class="actions"><button type="button" id="preview">Preview final name</button><button type="submit" ${editable?'':'disabled'}>Save edits</button></div><p id="name-preview" class="preview" hidden></p></form><div class="actions">${editable?'<button class="primary" id="approve">Save & approve</button><button id="now">Process now</button>':j.status==='READY'?'<button id="unqueue">Return to review</button>':''}</div><small>Approve follows the processing window. Process now overrides it for this book.</small></div><details class="panel"><summary>Activity history</summary>${j.events.map(e=>`<p><small>${new Date(e.created*1000).toLocaleString()}</small><br>${esc(e.message)}</p>`).join('')}</details></div></div>`;
$('#back').onclick=()=>act(queue);$('#group').onclick=()=>act(()=>grouping(j.package_id));$('#metadata-form').onsubmit=e=>{e.preventDefault();act(async()=>{await save();notice('Edits saved');await detail(id)})};$('#search').onclick=()=>act(async()=>{const button=$('#search');button.disabled=true;try{await api(`/jobs/${id}/search`,'POST',{provider:$('#provider').value,query:$('#query').value,author:$('#search-author').value,asin:$('#asin').value});await detail(id)}finally{button.disabled=false}});$('#embedded').onclick=()=>act(async()=>{await api(`/jobs/${id}/embedded`,'POST',{});await detail(id)});document.querySelectorAll('[data-candidate]').forEach(btn=>btn.onclick=()=>act(async()=>{await api(`/jobs/${id}/select`,'POST',{index:Number(btn.dataset.candidate)});await detail(id)}));$('#preview').onclick=()=>act(async()=>{const p=await api('/preview-name','POST',readMetadata());$('#name-preview').textContent=p.path;$('#name-preview').hidden=false});if($('#approve'))$('#approve').onclick=()=>act(()=>approve(false));if($('#now'))$('#now').onclick=()=>act(()=>approve(true));if($('#unqueue'))$('#unqueue').onclick=()=>act(async()=>{await api(`/jobs/${id}/review`,'POST',{});await detail(id)})}
function mediaMode(files){return files.every(f=>f.codec==='aac')&&new Set(files.map(f=>[f.sample_rate,f.channels,f.profile].join('|'))).size===1?'AAC · stream copy planned':'AAC conversion · processing window applies'}
function readMetadata(){return Object.fromEntries(fields.map(f=>[f,$(`#metadata-form [name="${f}"]`).value]))}
async function save(){await api('/jobs/'+current.id,'PUT',{metadata:readMetadata(),cover_choice:$('#metadata-form [name=cover]:checked')?.value||'none',grouping_confirmed:$('#group-confirm').checked})}
async function approve(force){await save();await api(`/jobs/${current.id}/approve`,'POST',{force_now:force});notice(force?'Queued for processing now':'Approved; the worker will follow your schedule');await queue()}
async function grouping(id){snapshot=await api('/jobs');const jobs=snapshot.jobs.filter(j=>j.package_id===id);if(jobs.some(j=>!['REVIEW','ERROR'].includes(j.status)))throw Error('Return all queued books in this package to review before regrouping. Completed/processing packages cannot be regrouped.');const excluded=await api(`/packages/${id}/excluded`);const rows=jobs.flatMap((j,i)=>j.body.files.map((f,n)=>({f,group:i+1,order:n+1}))).concat(excluded.map((f,i)=>({f,group:0,order:i+1})));screen('grouping');$('#grouping').innerHTML=`<button id="group-back">← Queue</button><h1>One package. The right books.</h1><p>Give files the same group number to combine them. Use 0 to explicitly exclude samples or extras. Order controls the final audio sequence.</p><div class="warning">Saving grouping resets metadata edits for this package. Search and review each new book afterward.</div><div class="panel"><div class="group-row"><strong>Source file</strong><strong>Book group</strong><strong>Order</strong></div>${rows.map((r,i)=>`<div class="group-row" data-row="${i}"><span class="path">${esc(r.f.relative)}</span><input aria-label="Book group for ${esc(r.f.relative)}" type="number" min="0" value="${r.group}" class="group-number"><input aria-label="Order for ${esc(r.f.relative)}" type="number" min="1" value="${r.order}" class="group-order"></div>`).join('')}<h3>Group titles</h3><p class="muted">One line per group: number = title. Add lines for any new groups.</p><textarea id="group-titles">${esc(jobs.map((j,i)=>`${i+1} = ${j.body.metadata.title}`).join('\n'))}</textarea><div class="actions"><button id="save-groups" class="primary">Confirm grouping & order</button></div></div>`;$('#group-back').onclick=()=>act(queue);$('#save-groups').onclick=()=>act(async()=>{const groups=new Map(),excluded=[];document.querySelectorAll('[data-row]').forEach(el=>{const n=Number(el.querySelector('.group-number').value),order=Number(el.querySelector('.group-order').value);if(!Number.isInteger(n)||n<0||!Number.isInteger(order)||order<1)throw Error('Use whole group and order numbers');const p=rows[Number(el.dataset.row)].f.path;if(n===0)excluded.push(p);else{if(!groups.has(n))groups.set(n,[]);groups.get(n).push({p,order})}});const titles=Object.fromEntries($('#group-titles').value.split('\n').map(line=>{const i=line.indexOf('=');return [line.slice(0,i).trim(),line.slice(i+1).trim()]}));const payload=[...groups].map(([n,items])=>{if(!titles[n])throw Error(`Enter a title for group ${n}`);if(new Set(items.map(x=>x.order)).size!==items.length)throw Error(`Group ${n} has duplicate order numbers`);return {title:titles[n],files:items.sort((a,b)=>a.order-b.order).map(x=>x.p)}});await api(`/packages/${id}/regroup`,'POST',{groups:payload,excluded});notice('Grouping saved. Review metadata for each book.');await queue()})}
const settingGroups=[['Folders & discovery','Only completed downloads belong in the source folder. Monitoring starts disabled so you can review your paths first.',['source_path','work_path','library_path','torrent_path','monitor_enabled']],['Metadata & confidence','Automation is conservative and off by default. Missing evidence earns zero points.',['auto_approve','confidence_threshold','confidence_margin','audible_region']],['Final naming','Use {title}, {author}, {series}, {series_number}, {year}, {year_prefix}, {year_suffix}, {series_suffix}. Empty series folders are omitted.',['folder_template','file_template','write_description','write_reader']],['Processing schedule','Heavy encodes start within this local-hour window; an active encode finishes. Equal start/end means all day. AAC remuxing can run anytime.',['timezone','heavy_start','heavy_end','aac_bitrate','ffmpeg_threads']],['qBittorrent','Optional torrent inbox submission. Save path is the path qBittorrent sees. Password remains unchanged when left blank.',['qbit_enabled','qbit_url','qbit_username','qbit_password','qbit_save_path']],['Audiobookshelf','Scan after a finalized batch. Failed scan requests retry independently. Token remains unchanged when left blank.',['abs_enabled','abs_url','abs_token','abs_library_id']]];
async function showSettings(){const {values:v,roots}=await api('/settings');screen('settings');$('#settings-form').innerHTML=settingGroups.map(([title,help,keys])=>`<div class="panel setting-group"><h2>${title}</h2><p class="muted">${help}</p><div class="field-grid">${keys.map(k=>typeof v[k]==='boolean'?`<label><input type="checkbox" name="${k}" ${v[k]?'checked':''}>${label(k)}</label>`:`<label>${label(k)}<input name="${k}" type="${['qbit_password','abs_token'].includes(k)?'password':typeof v[k]==='number'?'number':'text'}" value="${esc(v[k])}" ${v[k+'_configured']?'placeholder="Saved — leave blank to keep"':''}>${k.endsWith('_path')&&roots[k.replace('_path','').replace('torrent','torrents')]?`<small>Mounted root: ${esc(roots[k.replace('_path','').replace('torrent','torrents')])}</small>`:''}</label>`).join('')}</div></div>`).join('')+'<button class="primary" type="submit">Save Settings</button>';$('#settings-form').onsubmit=e=>{e.preventDefault();act(async()=>{const values={};for(const input of $('#settings-form').querySelectorAll('input'))values[input.name]=input.type==='checkbox'?input.checked:input.type==='number'?Number(input.value):input.value;await api('/settings','PUT',values);notice('Settings saved')})}}
$('#queue-nav').onclick=()=>act(queue);$('#settings-nav').onclick=()=>act(showSettings);$('#refresh').onclick=()=>act(queue);$('#filter').onchange=drawQueue;$('#discover').onclick=()=>act(async()=>{await api('/discover','POST',{});notice('Completed sources queued for inspection');await queue()});act(queue);
setInterval(()=>{if(!$('#queue').hidden)act(queue)},15000);
