# Next: dedicated review inbox

Requested 2026-09-26. A page containing only items that need review, with each item showing:

- Original title, author, narrator, duration, source filenames and embedded cover when available.
- Best metadata candidate alongside the original info, including its cover, edition details, score and explanation.
- **Approve** to accept that specific displayed match and queue normal processing.
- **Review** to open the full metadata, cover and grouping editor.

Highlight differences and conflicting evidence rather than making the user compare every field manually. Missing candidates should offer Review/search. Unconfirmed grouping must go through Review before approval; a quick approval must not accidentally merge a multi-book package. Approve follows the processing window and preserves the existing audit trail and per-field provenance. Use a stable candidate identifier to ensure the approved edition is exactly the one shown.

This is planned for a later version, not part of the matching/error-dismissal rebuild.
